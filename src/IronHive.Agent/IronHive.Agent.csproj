<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>IronHive Agent - Reusable agent layer for AI-powered CLI tools</Description>
    <!-- Disable CA1716: "Loop" namespace conflicts with reserved keyword in some languages -->
    <NoWarn>$(NoWarn);CA1716</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- Ironbees (multi-agent orchestration) -->
    <PackageReference Include="Ironbees.Core" />
    <!-- Microsoft.Extensions.AI -->
    <PackageReference Include="Microsoft.Extensions.AI" />
    <PackageReference Include="Microsoft.Extensions.AI.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.AI.OpenAI" />
    <PackageReference Include="ModelContextProtocol" />
    <PackageReference Include="NetEscapades.Configuration.Yaml" />

    <!-- OpenAI SDK -->
    <PackageReference Include="OpenAI" />

    <!-- DI -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.Extensions.Options" />

    <!-- Diff -->
    <PackageReference Include="DiffPlex" />

    <!-- Glob -->
    <PackageReference Include="Microsoft.Extensions.FileSystemGlobbing" />

    <!-- IndexThinking (token management, reasoning) -->
    <PackageReference Include="IndexThinking" />

    <!-- Security pin: forces patched Microsoft.Bcl.Memory (>=10.0.4) over transitive 9.0.4.
         Direct reference so the patched version flows to consumers, clearing NU1903 downstream. -->
    <PackageReference Include="Microsoft.Bcl.Memory" />
    <!-- MemoryIndexer (conversation memory, session management) -->
    <PackageReference Include="MemoryIndexer" />
    <PackageReference Include="MemoryIndexer.Sdk" />
  </ItemGroup>

  <ItemGroup>
    <!-- TokenMeter (token counting, multi-provider pricing) -->
    <PackageReference Include="TokenMeter" />
    <!-- IronHive (LLM abstraction) -->
    <PackageReference Include="IronHive.Abstractions" />
  </ItemGroup>

</Project>
